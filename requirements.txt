# Core
ipykernel==6.17.1 # colab
jupyter-client<8,>=5.3.4 # colab
numpy>=1.26
pandas>=2.2.0
pyarrow<20.0.0a0,>=14.0.0 # colab version limitation

# ducklake requires >= 1.3; 1.4 adds ducklake 0.3
duckdb==1.4.0
# requires pre-release for duckdb 1.4.0 support due to changing arrow return type for tables
ibis-framework[duckdb]>=10.8.1.dev76
ibis-framework[geospatial]

# LanceDB / Lance
lancedb>=0.20.0
pylance>=0.22.0

# Hamilton
sf-hamilton>=1.85.0

# Torch & Lightning (install torch based on your hardware from pytorch.org)
# install ML dependencies with conda like conda install pytorch torchvision to enable cuda or mps versions
lightning>=2.4
torchvision>=0.21.0
segmentation-models-pytorch>=0.3

# Experiment tracking
wandb>=0.19.0

# Geospatial (initial; may adjust as arro3 adoption is explored)
shapely>=2.0.7
geopandas>=1.0
h3>=4.1.2
# temporary dependencies; now that duckdb geography extension is merged for v1.4, we can remove AFTER reimplementation with duckdb geography
s2cell>=1.7
s2sphere

# Xarray and HF datasets for Core-Five
xarray>=2024.6
xarray[io]
zarr>=2.16
h5netcdf>=1.3
fsspec==2025.3.0 # colab
s3fs>=2024.12.0
huggingface_hub>=0.24
datasets>=4.0.0
obstore~=0.8.2
virtualizarr~=2.1.2
dask==2025.5.0 # colab

# Segment-geospatial (SamGEO)
segment-geospatial==0.13.0
segment-geospatial[samgeo2]

# Utilities
# rich>=13.7
tqdm>=4.66
ipywidgets~=8.1.7
python-dotenv~=1.1.1
graphviz~=0.21
matplotlib
seaborn
scikit-learn
# pydantic>=2.7

